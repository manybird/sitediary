import 'package:flutter/material.dart';

class Editor extends StatelessWidget {
  final EdgeInsets containerMargin =EdgeInsets.only(bottom: 8);
  final String title;
  Editor({this.title=''});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
